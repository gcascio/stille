import * as React from "react"

export function BrandText({
  className = '',
}: { className?: string; }) {
  return (
    <span className={`tracking-wider ${className}`}>
      stille
    </span>
  )
}
